# Example: Animation

Use /frontciti animate.

Add tasteful animation to the hero and feature reveal sections.

Requirements:

- Use CSS or existing motion tools for simple transitions.
- Use GSAP only if timelines or scroll coordination are needed.
- Respect reduced motion.
- Clean up animation lifecycle code.
- Keep performance smooth.
- Do not block content access.
